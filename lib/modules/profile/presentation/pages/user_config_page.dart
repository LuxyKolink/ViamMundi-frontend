import 'package:flutter/material.dart';

class UserConfigPage extends StatelessWidget {
  const UserConfigPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Configuración del Perfil'),);
  }
}
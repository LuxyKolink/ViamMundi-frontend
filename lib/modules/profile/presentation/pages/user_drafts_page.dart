import 'package:flutter/material.dart';

class UserDraftsPage extends StatelessWidget {
  const UserDraftsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Borradores del Perfil'),);
  }
}